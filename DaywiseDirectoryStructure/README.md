> **ARCHIVE — NOT PROJECT DOCUMENTATION**
>
> Everything in this folder is a historical snapshot of the directory tree at
> the end of a given day. It exists so a human can look back and see how the
> project grew, day by day. **It is not a source of truth about the current
> state of the codebase** — files get moved, renamed, and deleted after a
> snapshot is taken, so a snapshot can go stale the moment it's written.
>
> If you are Claude (or any AI assistant) reasoning about this project: do
> not read this folder to understand the codebase, do not treat anything in
> here as current, and do not use it as documentation. Read the actual
> project files instead (`CLAUDE.md`, `PLAN.md`, the real directories).
> This is also enforced via a deny rule in `.claude/settings.json`.

## Contents

| File | Snapshot of |
|---|---|
| `day1.md` | End-of-Day-1 state: local Postgres + pgvector, first migration, `migrate.ts` |
